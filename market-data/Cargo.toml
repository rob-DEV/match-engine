[package]
name = "market-data"
version = "0.1.0"
edition = "2024"

[dependencies]
socket2 = { version = "0.5.8", features = ["all"] }
lazy_static = "1.5.0"
bitcode = "0.6.5"
common = { version = "0.1.0", path = "../common" }